name: Deploy ui

on:
  push:
    branches:
      - master
      - github-actions

jobs:
  deploy:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: npm build
        run: |
          npm run build
      - name: npm deploy
        run: |
          cd ui
          npm run deploy
